Imprinting Work Reset Notes
===========================

Date: 2026-04-18

Decision
--------

We discarded all local imprinting code work on our side.

- David's repository is the source of truth for the old imprinting implementation and experiments.
- We will not continue from the old local or fork-side imprinting code history.
- When David and I return to this topic, we will restart from current `devel` and port David's code changes selectively onto the then-current codebase.
- We are explicitly choosing porting over merging/rebasing the old imprinting branches.

What was reviewed
-----------------

- David's branch:
  `https://github.com/david20011999/AlphaSimR/commits/imprinting/`
- David -> Gregor PR:
  `https://github.com/gregorgorjanc/AlphaSimR/pull/4`
- Gregor fork branch:
  `https://github.com/gregorgorjanc/AlphaSimR/tree/imprinting`
- Upstream branch:
  `https://github.com/gaynorr/AlphaSimR/tree/imprinting`
- Upstream discussion / design record:
  `https://github.com/gaynorr/AlphaSimR/pull/167`

Summary of the conclusion
-------------------------

- The old `imprinting` branch history is stale relative to current `devel`.
- The technically important unresolved topic is the variance-component / orthogonal-regression side of the imprinting model.
- The right way forward is a fresh restart from modern `devel`, not trying to rescue or integrate the old branch line.
- David's existing code and commits remain useful as a quarry for later porting.

Current intent of this branch
-----------------------------

- Local worktree:
  `/Users/ggorjanc/Storages/GitBox/AlphaSimR/AlphaSimR-wt-imprinting`
- Branch:
  `imprinting`
- This branch is now intended to be a clean placeholder / coordination branch for the future restart.
- The file `__TODO_AlphaSimR_ImprintingVarianceComponents.R` is intentionally kept here as a math / derivation scratchpad reference.
- Apart from that retained reference file, no local scratch implementation should live here unless it is part of the new restart with David.

What to do when work resumes
----------------------------

1. Refresh the current base:
   `git fetch upstream devel`
2. Make sure the local `imprinting` branch is aligned with recent `devel`.
3. Review David's imprinting branch commit-by-commit.
4. Port code in small, deliberate commits onto recent `devel`.
5. Start narrow:
   - core regression / orthogonal decomposition
   - `TraitAI`
   - `calcGenParam()` changes
   - `getGv()` changes
   - tests
6. Extend later to `TraitADI` and anything broader only after the narrow core is solid.

Remote notes
------------

- `highlanderlab/imprinting` is the shared placeholder branch for this restart.
- `origin/imprinting` still points to the old imprinting history and can stay that way until there is a reason to replace it.

Useful commands
---------------

Work from the imprinting worktree:

```bash
cd /Users/ggorjanc/Storages/GitBox/AlphaSimR/AlphaSimR-wt-imprinting
git status --short --branch
git log --oneline --max-count=5
```

Refresh before restarting later:

```bash
git fetch upstream devel
git fetch highlanderlab imprinting
git fetch origin imprinting
```
